import React from 'react';
import {Text, View} from 'react-native';

const Base = ()=>{
    return <View className={'bg-white rounded p-1'}>
        <Text className={'font-bold text-xl text-blue-500'}>Hello 222345</Text>
        <Text className={'font-bold text-xl text-green-700'}>It`s a-car</Text>
    </View>;
};

export default Base;
